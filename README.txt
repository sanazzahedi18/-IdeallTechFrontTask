IdeallTechFrontTask Documentation:

Setup:

1. Clone the repository
2. Run (npm i) in the root of the project on (main) branch

Run instructions:

1. Run (npm run dev) in the root of the project
2. Open localhost:3000 in a browser

*. I will provide the env file in the git repository because there is no api key in the 
env file